
<div class="codeExample topSpace">
    <label class="codeLabelBlue">CODE:</label>
    <div class="codeTitle codeTitleBlue">Be sure to import the SeedPacket namespace.</div>
<code>
<i>// Defaults to 10 rows</i>
var items = new List&lt;User&gt;().Seed();

<i>// Create 20 rows</i>
var items = new List&lt;User&gt;(20).Seed();

<i>// Create rows starting with 100 to 200</i>
var items = new List&lt;User&gt;(100, 200).Seed();
</code>
</div>


