
<div class="codeExample topSpace">
<label class="codeLabelBlue">CODE:</label>
<div class="codeTitle codeTitleBlue">Be sure to import the <b>SeedPacket.Extensions</b> namespace.</div>
<code>
<i>// Creates 10 rows (default)</i>
var users = new List&lt;User&gt;().Seed();
    
<i>// Creates 20 rows using new IGenerator</i>
var users = new List&lt;User&gt;().Seed(1, 20, new MultiGenerator());

<i>// Create 1000 rows by count</i>
var users = new List&lt;User&gt;().Seed(1000);

<i>// Create rows starting with 100 to 200 with custom datasource file, either XML or JSON.</i>
var users = new List&lt;User&gt;().Seed(100, 200, "~/Sources/JSONSource.json");
</code>
</div>


