
<div class="codeExample topSpace">
    <label class="codeLabelBlue">CODE:</label>
    <div class="codeTitle codeTitleBlue">Be sure to import the <b>SeedPacket</b> namespace.</div>
<code>
<i>// Gets data from the referenced .xml file</i>
var ss = new SimpleSeed("~/SourceFiles/seedsource.xml");
var userList = Enumerable.Range(100, 20)
.Select(n => new User {
        UserId    = n,
        FirstName = ss.Next("FirstName", n),
        LastName  = ss.Next("LastName", n),
        Email     = ss.Next("FirstName", n).FirstOrDefault() + 
                    ss.Next("LastName", n) + "@@" +
                    ss.Next("CompanyName", n) +
                    ss.Next("DomainExtension", n),
        Created   = DateTime.Now.AddMonths(n)
}).ToList();
</code>
</div>


