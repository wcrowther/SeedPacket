@using Examples.Models

@model  List<FootballGame>

<div class="row">
    <div class="col-md-12">
        <table class="table accountTable table-striped topSpace table-collapse">
            <thead>
                <tr>
                    <th>Home Team</th>
                    <th></th>
                    <th>Visiting Team</th>
                </tr>
            </thead>
            <tbody>
                @for (int i = 0; i < Model.Count; i++)
                {
                    var game = Model[i];
                    <tr>
                        <td>
                            <div style="float:left; font-weight: bold; color: #346194;">@(i + 1)</div>
                            <div style="text-align:right;">@game.HomeTeam</div>
                        </td>
                        <td style="text-align:center;">@game.GameDate.ToString("MM/dd/yyyy h:mm tt")</td>
                        <td>@game.AwayTeam</td>
                    </tr>
                }
            </tbody>
         </table>
     </div>
</div>
