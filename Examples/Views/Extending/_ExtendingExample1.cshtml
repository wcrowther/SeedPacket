@using Examples.Models
@using Examples.Extensions

<div class="row topSpace">
    <div class="col-md-12">
        <table class="table table table-striped topSpace table-collapse-sm">
            <thead>
                <tr>
                    <th></th>
                    <th>Title</th>
                    <th>Album</th>
                    <th>Artist</th>
                    <th>Released</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var song in new List<Song>().Seed().ToList())
                {
                    <tr>
                        <td>@song.Ranking</td>
                        <td>@song.SongTitle</td>
                        <td>@song.AlbumName</td>
                        <td>@song.Artist</td>
                        <td>@song.Released.ToShortDateString()</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>